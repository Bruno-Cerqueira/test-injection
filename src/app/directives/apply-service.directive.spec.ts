import { ApplyServiceDirective } from './apply-service.directive';

describe('ApplyServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyServiceDirective();
    expect(directive).toBeTruthy();
  });
});
